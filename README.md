# SimulCast — AI 同声传译助手

> 七牛云 × XEngineer 暑期实训营 · 题目二：AI 同声传译

**SimulCast** 是一款浏览器端的 AI 同声传译工具，帮助用户实时理解外语演讲、技术分享和国际会议内容。打开浏览器即可使用，无需安装任何客户端。

## ✨ 核心特性

### 🎬 视频搬运模式

捕获浏览器标签页的视频和音频，将视频画面搬到我们的页面上显示，字幕叠加在视频下方。用户可以在一个页面内同时观看视频和翻译字幕，无需在多个窗口之间切换。

### 🌐 实时语音翻译

使用阿里云百炼 LiveTranslate 模型，采用端到端架构，语音直接翻译成中文，无需中间步骤。支持流式输出，逐字显示翻译结果，延迟约 1 秒。

### 🔍 智能纠错

LiveTranslate 内置纠错能力，当检测到之前识别或翻译有误时，会自动修正并以视觉提示告知用户。

### 🌍 多语言支持

支持 6 种语言的实时翻译：英语、日语、韩语、法语、德语、西班牙语，覆盖主流外语场景。

### 📄 历史记录与导出

翻译过程中的每一句话实时显示在历史面板中，支持流式更新。翻译结束后，可选择导出为 PDF、Word 或 TXT 三种格式，方便保存和分享。

### 🎨 科技感界面

采用极光渐变、音波流动、粒子漂浮三层动态背景，搭配毛玻璃字幕效果，营造沉浸式的翻译体验。

## 🚀 快速开始

### 环境要求

- Python 3.9+
- 阿里云百炼 API Key（[申请地址](https://dashscope.console.aliyun.com/)）

### 安装步骤

```bash
# 1. 克隆仓库
git clone https://github.com/muonuo/ai-simultaneous-interpreter.git
cd ai-simultaneous-interpreter

# 2. 创建虚拟环境
python -m venv venv
venv\Scripts\activate  # Windows
# source venv/bin/activate  # macOS/Linux

# 3. 安装依赖
pip install -r requirements.txt

# 4. 配置 API Key
copy .env.example .env
# 编辑 .env，填入你的 DASHSCOPE_API_KEY

# 5. 启动服务
python main.py
```

启动后打开浏览器访问 `http://127.0.0.1:8000`

## 🏗️ 系统架构

```
┌─────────────────────────────────────────────────────────┐
│                       浏览器端                           │
│                                                         │
│   用户操作          音频采集           画面渲染           │
│   ┌─────────┐     ┌─────────┐      ┌─────────────┐     │
│   │ 选择标签页│────▶│ 采集音频 │─────▶│ 视频 + 字幕  │     │
│   └─────────┘     └────┬────┘      └─────────────┘     │
│                        │                                │
└────────────────────────┼────────────────────────────────┘
                         │ 音频流（实时传输）
                         ▼
              ┌─────────────────────┐
              │     服务器端         │
              │                     │
              │  接收音频流          │
              │  转发到翻译引擎      │
              │  返回翻译结果        │
              │                     │
              └──────────┬──────────┘
                         │
                         ▼
              ┌─────────────────────┐
              │  阿里云 LiveTranslate │
              │                     │
              │  语音识别 → 翻译     │
              │  （端到端，一步完成） │
              │  内置纠错能力        │
              └─────────────────────┘
```

**工作流程：**

1. 用户在浏览器中选择要翻译的标签页（如 YouTube 英文视频）
2. 浏览器采集标签页的音频，实时发送到服务器
3. 服务器将音频转发给阿里云 LiveTranslate 模型
4. LiveTranslate 同时完成语音识别和翻译，返回中文结果
5. 浏览器收到翻译结果，以字幕形式叠加在视频下方

## 💡 设计思路

### 为什么选择浏览器方案？

我们对比了三种技术路线：

| 方案 | 优点 | 缺点 |
|------|------|------|
| Electron 桌面客户端 | 原生体验，可打包为 .exe | 需要安装，仅限桌面端 |
| PyQt 桌面客户端 | 支持系统音频捕获 | 仅限 Windows，需要 Python 环境 |
| **Web 浏览器** | **零安装，全平台支持，手机平板也能用** | 仅能捕获标签页音频 |

我们选择了 Web 方案，因为 AI 同声传译是高频但非刚需的工具，用户不愿意为了偶尔使用而安装软件。浏览器打开即用，使用门槛最低。

### 为什么选择 LiveTranslate？

我们尝试了三种语音翻译方案：

| 方案 | 延迟 | 准确率 | 纠错 |
|------|------|--------|------|
| 浏览器内置语音识别 | 低 | 低，特别是专业术语 | 无 |
| 语音识别 + 文本翻译（两步） | 3-5 秒 | 高 | 需要额外开发 |
| **阿里云 LiveTranslate（端到端）** | **约 1 秒** | **高** | **内置纠错** |

LiveTranslate 是端到端模型，语音直接翻译成目标语言，无需先识别再翻译，延迟最低且内置纠错能力。

### 为什么设计视频搬运模式？

我们尝试了三种字幕显示方案：

| 方案 | 问题 |
|------|------|
| 弹窗模式 | 窗口边框不美观，需要手动调整位置 |
| 画中画模式 | 浏览器兼容性差，样式不可控 |
| **视频搬运模式** | **视频和字幕在同一个页面，一体化体验** |

视频搬运模式让用户在一个页面内同时看到视频画面和翻译字幕，无需在多个窗口之间切换，体验最流畅。

### 如何实现智能纠错？

语音识别和翻译都可能出错，特别是长句子或专业术语。LiveTranslate 会在翻译过程中发现之前的错误，并返回修正后的结果。我们在前端通过视觉提示（字幕闪烁）告知用户当前内容已被修正，让用户知道系统在持续优化翻译质量。

### 如何采集浏览器音频？

浏览器提供了屏幕捕获 API，可以获取标签页的视频画面和音频。我们使用音频处理技术将捕获的音频转换为 LiveTranslate 需要的格式（16kHz 采样率），并通过 WebSocket 实时传输到服务器。整个过程在浏览器端完成，音频数据不会存储在本地。

## 📦 技术栈

| 类别 | 技术 | 用途 |
|------|------|------|
| Web 框架 | FastAPI | 后端服务 |
| 实时通信 | WebSocket | 音频流传输和翻译结果接收 |
| 翻译引擎 | 阿里云 LiveTranslate | 语音实时翻译 |
| 音频采集 | Web Audio API | 浏览器端音频采集 |
| 视频捕获 | getDisplayMedia | 标签页视频和音频捕获 |
| PDF 导出 | jsPDF + html2canvas | 客户端 PDF 生成 |

## 📁 项目结构

```
ai-simultaneous-interpreter/
├── main.py                # 服务入口
├── translator_live.py     # 翻译引擎客户端
├── config.py              # 配置管理
├── requirements.txt       # Python 依赖
├── .env.example           # 环境变量模板
├── static/
│   ├── index.html         # 主页面
│   ├── app.js             # 前端逻辑
│   └── style.css          # 样式
└── README.md
```

## 🔒 安全说明

- API Key 通过环境变量管理，不暴露到前端
- 静态文件服务添加路径遍历防护
- 音频数据实时处理，不存储

## 📄 开源协议

MIT License

## 🙏 致谢

- 阿里云百炼 LiveTranslate 提供实时翻译能力
- 七牛云 × XEngineer 暑期实训营提供比赛机会
